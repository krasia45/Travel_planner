# 🗺️ AI 국내 복수 여행지 추천 및 자동 폴백(Fallback) 플래너
> **사용자가 입력한 특정 날짜를 기반으로 AI(Gemini) 모델이 국내 최적의 여행지를 큐레이션하고, 실시간 네이버 검색 API의 로컬 장소 데이터와 매시업(Mashup)하여 신뢰성 높은 여행 계획서를 자동 생성하는 백엔드 프로그램입니다.**

본 프로젝트는 AI 프롬프트 엔지니어링, 복수 데이터 파싱 아키텍처 구축, 외부 API 연동, 그리고 네트워크 장애에 대응하는 안정적인 **폴백(Fallback) 방어 프로그래밍**까지 백엔드 엔지니어가 거쳐야 할 필수적인 시스템 설계 과정을 집약하여 수행되었습니다.

---

## 🎯 1. 과제 목표 및 핵심 학습 성과

본 프로그램을 설계하고 고도화하며 달성한 핵심 기술적 목표는 다음과 같습니다.

* **REST API 및 HTTP 메서드의 이해**: 무상태성(Stateless) 통신 구조 하에서 데이터 생성을 위한 `POST` 요청(Gemini API)과 데이터 조회를 위한 `GET` 요청(Naver API)의 차이점을 명확히 이해하고 헤더 및 파라미터를 다르게 구성하여 구현했습니다.
* **LLM 출력 구조화(JSON Parsing)**: 비정형 텍스트를 반환하는 LLM의 특성을 통제하기 위해 프롬프트 엔지니어링을 적용하여 완전한 JSON 포맷 출력을 강제했습니다. 이를 통해 1차 데이터를 파싱하여 다음 파이프라인(네이버 장소 검색)의 입력값으로 사용하는 **데이터 연동 파이프라인**을 구축했습니다.
* **예외 처리 및 운영 안정성 확보**: 네트워크 단절, API 인증 실패(401/403), 쿼터 초과, 파싱 에러 발생 시 프로그램이 비정상 종료되지 않도록 `try-except` 예외 처리 및 재시도(Retry) 로직을 설계했습니다. 특정 API 실패 시에도 가용한 데이터를 바탕으로 최종 마크다운 리포트를 끝까지 생성합니다.
* **보안 내재화 (Environment Variables)**: 금융적 비용이 발생할 수 있는 소중한 API Secret Key의 유출을 방지하기 위해 코드 내 하드코딩을 원천 차단하고 `.env` 환경변수 관리 방식을 엄격히 적용했습니다.

---

## 💻 2. 개발 및 실행 환경

* **언어**: Python 3.10 이상
* **인터페이스**: 터미널 기반 CLI (Command Line Interface)
* **필수 라이브러리**: `requests`, `google-genai`, `python-dotenv`

---

## 🏗️ 3. 전체 시스템 아키텍처 및 데이터 흐름 (Data Flow)



본 프로그램은 단방향 흐름으로 동작하는 CLI 애플리케이션으로, 수집된 데이터를 최종적으로 정형화된 JSON 파일과 가독성 높은 마크다운(Markdown) 문서로 변환하여 저장합니다. 전체적인 프로세스 파이프라인은 다음과 같습니다.

1.  **사용자 입력 인터페이스 (Argument CLI):** 파이썬의 `argparse` 모듈을 통해 `YYYY-MM-DD` 포맷의 정격 날짜 인자를 수집 및 검증합니다.
2.  **AI 컨텍스트 분석 및 큐레이션 (LLM Processing):** 구글의 `gemini-2.5-flash` 모델을 활용하여 해당 날짜의 계절성, 기후 트렌드, 지역 축제 정보를 다각도로 분석한 후 복수의 최적 후보지를 JSON 구조체로 도출합니다.
3.  **실시간 로컬 데이터 매시업 (Data Mashup):** 분석된 후보 도시들을 식별자로 삼아 네이버 지역 검색 API를 동기식(Synchronous)으로 호출, 대중적인 실시간 맛집 리스트를 동적으로 결합합니다.
4.  **방어적 예외 제어 (Robust Fallback):** 네이버 API의 제한 초과나 데이터 공백 발생 시, 시스템 셧다운을 방지하고 서비스 연속성을 확보하기 위해 LLM의 도메인 지식 기반 백업 데이터로 즉시 전환(Fallback)합니다.
5.  **영속성 저장 및 최종 가공 (Persistence & Export):** 정형 데이터 보존을 위한 원본 JSON 스토리지 저장과 사용자 최종 인도용 마크다운 리포트 변환 작업을 병렬로 수행합니다.

---

## 🛠️ 4. 문제 해결 및 단계별 구현 과정 (Core Workflow)

과제를 성공적으로 완수하고 아키텍처를 고도화하기 위해 거친 핵심 개발 아젠다와 구현 과정입니다.

### [1단계] CLI 인터페이스 및 입력값 검증 (`argparse`)
* `argparse` 라이브러리를 사용해 `--date "YYYY-MM-DD"` 형태의 필수 옵션을 안전하게 처리하도록 구현했습니다.
* 정규표현식(`re`) 및 `datetime` 모듈을 연동하여, 사용자가 날짜 형식을 잘못 입력할 경우 직관적인 에러 가이드라인을 출력하고 안전하게 프로그램을 종료하도록 방어 코드를 작성했습니다.

### [2단계] `.env` 기반의 보안 강화 및 로드 트러블슈팅
* `.env` 파일을 생성하여 `GEMINI_API_KEY`, `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`을 격리했습니다.
* 개발 중 환경변수가 정상적으로 로드되지 않아 API 인증 에러(401)가 발생하는 현상을 겪었습니다. 이를 해결하기 위해 파일 경로를 절대 경로로 추적하거나 `dotenv.load_dotenv()`의 실행 시점을 프로그램 최상단으로 끌어올려 외부 유출 없는 안전한 인증 구조를 완성했습니다.

### [3단계] Gemini API 연동 및 JSON 구조화 처리
* 날짜 데이터를 프롬프트에 실어 Gemini 모델에 `POST` 요청을 보냈습니다.
* AI가 임의의 설명글을 붙여 JSON 파싱 에러를 유도하는 문제를 해결하기 위해, 프롬프트에 **"반드시 markdown 블록 없이 순수 JSON 스키마로만 응답하라"**는 제약 조건을 걸어 안전한 1차 JSON 객체를 확보했습니다.

### [4단계] 네이버 지역 검색 API 연동 및 데이터 토스
* 1차 JSON에서 파싱한 `recommended_city`를 키워드로 삼아 네이버 Local 검색 API에 `GET` 요청을 전송했습니다.
* 네이버 API 규격에 맞춰 `X-Naver-Client-Id`와 `X-Naver-Client-Secret`을 HTTP 헤더에 정확히 실어 나르도록 구현했으며, 응답받은 맛집 목록 중 상위 3곳의 필드를 정제해 바인딩했습니다.

### [5단계] 다원화된 에러 로깅 세션 추적 시스템 구현
* 프로그램 가동 중에 발생하는 경고나 외부 API 실패 현상을 실시간으로 추적하고, 최종 리포트에 고스란히 담아 개발자 및 사용자가 시스템 신뢰도를 모니터링할 수 있어야 했습니다.
* 글로벌 에러 추적 스택 배열(`ERROR_LIST`)을 선언하고, 단계별로 예외가 발생할 때마다 어떤 레이어(`Naver API`, `LLM Generation` 등)에서 어떤 유형의 문제가 생겼는지 정형화된 규격으로 기록하는 `record_error()` 유틸리티 함수를 빌드했습니다. 프로그램이 치명적인 이슈로 강제 종료되는 순간에도 `save_emergency_json()` 비상 저장 모듈이 가동되어 그때까지 쌓인 로그를 파일 시스템에 안착시킵니다.

---

## ✨ 5. 보너스 과제 최종 성과 요약

기본 과제 요건을 넘어, 실제 상용 서비스 수준의 안정성과 가독성을 갖추기 위해 아래 2가지 보너스 과제를 통합 설계했습니다.

### 1️⃣ 복수 지역 추천 (Multi-Destination Support) [보너스 1 완료]
* **기능 설명:** 기존의 단 한 곳만 추천하던 단조로운 방식에서 벗어나, AI가 입력된 날짜의 계절성과 지역 축제 트렌드를 분석한 뒤 **가장 최적의 후보 도시를 2~3곳 선정하여 리스트 배열 형태로 제공**합니다. 
* **구현 방식:** 프롬프트 내부 구조를 JSON Array 형태로 강제하고, 파이썬 코드 내에서 `for` 반복문(루프)을 가동하여 선정된 복수의 도시 정보와 맛집 데이터를 각각 독립적으로 매칭 및 누적 처리합니다.

### 2️⃣ 시스템 예외 대비 자동 폴백 시스템 (Robust Fallback Mechanism) [보너스 2 완료]
* **기능 설명:** 외부 API 환경은 트래픽 급증, 인증 만료, 순간적인 네트워크 단절 등으로 언제든 실패(Failure)할 위험이 존재합니다. 맛집 검색이 실패하더라도 시스템이 폭파되거나 리포트가 비어버리는 배드 유저 익스피리언스(Bad UX)를 원천 차단하고자 했습니다.
* **구현 방식:** 네이버 API 조회 결과가 비어있음을 감지하는 즉시 **`Fallback Triggered` 예외 세션 로그**를 작동시킵니다. 그리고 **AI(Gemini)가 프롬프트 생성 단계에서 미리 확보해 둔 '도시별 도메인 기반 자체 백업 맛집/특산물 정보(`fallback_restaurants`)'를 메모리 상에서 즉시 대체 결합(Binding)하도록 구현하여 무중단 환경을 완성했습니다.

---

## 🚀 6. 실행 방법 및 결과물 확인 (Mac OS 환경 기준)

추후 프로젝트를 다시 다운로드받거나 다른 환경에서 재실행할 때, 맥북 고유의 시스템 파이썬 설치 차단 에러(`externally-managed-environment`)를 우회하여 가상환경을 안전하게 구동하는 절대 실패 없는 가이드라인입니다.

### 1) 프로젝트 폴더 정리 및 열기
1. 깃허브에서 다운로드받은 파일들을 그대로 두지 말고, 다운로드 폴더나 바탕화면에 **`Travel`**이라는 이름의 새 폴더를 만듭니다.
2. `travel_planner.py` 파일을 생성한 `Travel` 폴더 안으로 이동시킵니다.
3. **VS Code**를 실행한 뒤, `[File]` ➡️ `[Open Folder...]`를 통해 **`Travel` 폴더를 통째로 선택해서 열어줍니다.** (왼쪽 탐색기에 파일이 바로 보여야 경로가 맞게 잡힌 것입니다.)

### 2) 터미널에서 가상환경 구축 및 강제 패키지 설치
VS Code 상단 메뉴에서 `[Terminal]` ➡️ `[New Terminal]`을 눌러 새 터미널 창을 열고, 아래 명령어를 **한 줄씩 순서대로 입력하고 엔터**를 누릅니다.

```bash
# 1. venv라는 이름의 새로운 가상환경 폴더를 생성합니다.
python3 -m venv venv

# 2. 맥북에서 가상환경을 강제로 활성화(연결)합니다. (성공 시 맨 왼쪽에 (venv)가 표시됨)
source venv/bin/activate

# 3. 맥북 전역 시스템의 간섭 에러를 무시하고 가상환경 내부에 패키지를 강제 설치합니다.
pip install python-dotenv requests google-genai --break-system-packages


## 3. 환경 변수 파일(.env) 생성 및 비밀 키 설정

1. VS Code 왼쪽 **파일 탐색기(File Explorer)**의 빈 공간을 마우스 우클릭합니다.
2. **New File**을 선택한 후 `.env` 파일을 생성합니다.
3. 생성한 `.env` 파일에 아래 내용을 입력한 뒤 저장합니다. (`Command + S` 또는 `Ctrl + S`)

```env
GEMINI_API_KEY="본인의_구글_제미나이_API_KEY"
NAVER_CLIENT_ID="본인의_네이버_클라이언트_ID"
NAVER_CLIENT_SECRET="본인의_네이버_클라이언트_SECRET"
```

> **⚠️ 보안 주의사항**
>
> `.env` 파일에는 개인 API 비밀 키가 저장되므로 **절대로 GitHub에 업로드하면 안 됩니다.**
>
> 반드시 `.gitignore` 파일에 `.env`가 포함되어 있는지 확인하여 Git 저장소에 커밋되지 않도록 설정하세요.

---

# 🛠️ Git 저장 및 GitHub 업로드 방법 (초보자용 가이드)

아래 순서대로 진행하면 프로젝트를 안전하게 GitHub 원격 저장소에 업로드할 수 있습니다.

---

## ⚠️ 1. `.gitignore` 파일 생성 (필수)

프로젝트에는 API 비밀 키(`.env`)와 Python 가상환경(`venv/`)이 포함되어 있으므로 **절대로 GitHub에 업로드하면 안 됩니다.**

프로젝트 폴더(`Travel_planner`)에 **`.gitignore`** 파일을 생성한 뒤 아래 내용을 입력하고 저장합니다.

```text
.env
venv/
```

> **💡 왜 필요한가요?**
>
> - `.env` : API Key와 같은 민감한 정보를 보호합니다.
> - `venv/` : 가상환경은 용량이 크고 다른 환경에서 다시 생성할 수 있으므로 저장소에 포함하지 않습니다.

---

## 2. Git 저장 및 GitHub 업로드

터미널이 프로젝트 폴더(`Travel_planner`)를 가리키고 있는지 확인한 후 아래 명령어를 **순서대로 한 줄씩 실행**합니다.

### ① Git 저장소 초기화

```bash
git init
```

현재 프로젝트를 Git으로 관리하기 시작합니다.

---

### ② 변경된 파일 추가

```bash
git add .
```

프로젝트의 모든 파일을 Git의 스테이징 영역(Stage)에 추가합니다.

---

### ③ 첫 번째 커밋 생성

```bash
git commit -m "Feat: 국내 복수 여행지 추천 및 Fallback 로직 구현 완료 (과제 제출용)"
```

현재 프로젝트 상태를 하나의 버전으로 저장합니다.

---

### ④ 기본 브랜치 이름 변경

```bash
git branch -M main
```

기본 브랜치를 `main`으로 설정합니다.

---

### ⑤ GitHub 저장소 연결

GitHub에서 새로운 Repository를 생성한 후 저장소 주소를 복사하여 아래 명령어에 입력합니다.

```bash
git remote add origin https://github.com/본인의계정이름/레포지토리이름.git
```

예시

```bash
git remote add origin https://github.com/example-user/Travel_planner.git
```

---

### ⑥ GitHub에 최종 업로드

```bash
git push -u origin main
```

프로젝트를 GitHub 원격 저장소에 업로드합니다.

---

## ✅ 전체 명령어 모음

아래 명령어를 순서대로 실행하면 됩니다.

```bash
git init

git add .

git commit -m "Feat: 국내 복수 여행지 추천 및 Fallback 로직 구현 완료 (과제 제출용)"

git branch -M main

git remote add origin https://github.com/본인의계정이름/레포지토리이름.git

git push -u origin main
```

---

## 🎉 업로드 완료

정상적으로 완료되면 GitHub 저장소에서 프로젝트 파일을 확인할 수 있습니다.

이후 프로젝트를 수정한 경우에는 아래 세 명령어만 반복하면 변경 사항을 GitHub에 반영할 수 있습니다.

```bash
git add .

git commit -m "수정 내용"

git push
```

## 4. 프로그램 최종 실행

모든 환경 설정이 완료되었다면 터미널에서 아래 명령어를 실행합니다.

> **📌 날짜 입력 규칙**
>
> 날짜는 반드시 `YYYY-MM-DD` 형식을 사용해야 합니다.
>
> 월(`MM`)과 일(`DD`)이 한 자리인 경우에도 반드시 앞에 `0`을 붙여 **10자리 형식**을 유지해야 합니다.
>
> **예시**
>
> - `2026-09-05` ✅
> - `2026-10-15` ✅
> - `2026-9-5` ❌

```bash
# 실행 예시
python travel_planner.py --date "2026-10-15"
```

---

# 📂 7. 영속성 결과물(Persistence Output) 구조

프로그램이 정상적으로 실행되면 `results/` 디렉터리에 입력한 날짜를 기준으로 아래 두 개의 결과 파일이 자동 생성됩니다.

## 1. 백엔드 원본 데이터(JSON)

```text
results/travel_report_YYYY-MM-DD.json
```

이 파일에는 다음 정보가 포함됩니다.

- 복수 추천 여행지 정보
- 네이버 API 검색 결과
- API 실패 시 자동 적용된 폴백(Fallback) 장소 정보
- 프로그램 실행 중 발생한 오류 및 경고(`errors`) 기록
- 최종 생성된 백엔드 원본 데이터

---

## 2. 사용자용 여행 리포트(Markdown)

```text
results/YYYY-MM-DD_travel_plan.md
```

JSON 데이터를 사람이 읽기 쉽도록 가공한 여행 리포트입니다.

VS Code에서 해당 파일을 연 뒤 우측 상단의 **Open Preview** 아이콘을 클릭하면 매거진 형태의 마크다운 문서로 확인할 수 있습니다.

> **💡 Tip**
>
> Markdown Preview 기능을 사용하면 이미지, 표, 제목 등이 보기 좋게 렌더링되어 최종 결과물을 더욱 편리하게 확인할 수 있습니다.










# 🗺️ 2026-11-15 국내 여행 추천 리포트

## 📍 후보 1: 전주
### 🌤️ 날씨 요약
- 쌀쌀하고 건조한 늦가을 날씨로, 낮에는 활동하기 좋으나 아침저녁으로는 서늘합니다.

### 🥳 주요 행사/축제
- 전주 한옥마을 상설 문화 체험
- 전주 남부시장 야시장 (금, 토 운영)

### 💡 추천 이유
11월 중순의 전주는 한옥마을의 고즈넉한 정취와 전통 문화를 깊이 체험하기에 이상적입니다. 비빔밥, 콩나물국밥 등 전주 특유의 풍성한 먹거리를 즐기며 쌀쌀한 날씨 속에서 따뜻한 한옥의 매력을 만끽하기 좋습니다. 단풍은 대부분 졌겠지만, 고풍스러운 한옥과 어우러진 가을 끝자락의 풍경이 아름답습니다.

### 🍕 추천 맛집 및 먹거리
- **평화와평화** (음식점>카페,디저트) - *주소: 전북특별자치도 전주시 완산구 전라감영4길 16-7 3층*
- **베테랑 칼국수** (분식>종합분식) - *주소: 전북특별자치도 전주시 완산구 경기전길 135*
- **카페이르리** (음식점>카페,디저트) - *주소: 전북특별자치도 전주시 완산구 은행로 69*

---

## 📍 후보 2: 부산
### 🌤️ 날씨 요약
- 해양성 기후로 내륙보다 비교적 온화하나, 바닷바람이 쌀쌀하게 느껴질 수 있습니다. 맑고 청량한 날씨가 예상됩니다.

### 🥳 주요 행사/축제
- 자갈치 시장
- 국제시장
- 광복동 트리 축제 (시기상 시작될 가능성 있음)

### 💡 추천 이유
늦가을 부산은 해양 도시 특유의 활기찬 분위기와 함께 상대적으로 온화한 날씨를 즐길 수 있습니다. 아름다운 해변과 해안 풍경을 감상하며 따뜻한 돼지국밥이나 신선한 해산물을 맛보기에 좋습니다. 광복동 트리 축제 등 연말 분위기가 시작될 수도 있어 다채로운 매력을 선사합니다.

### 🍕 추천 맛집 및 먹거리
- **도우개러지 피자 전포점** (음식점>피자) - *주소: 부산광역시 부산진구 서전로46번길 8 1층 도우개러지*
- **조개대표 광안본점** (한식>조개요리) - *주소: 부산광역시 수영구 광남로94번길 16 204호*
- **솔내음한정식 해운대본점** (한식>한정식) - *주소: 부산광역시 해운대구 달맞이길117번길 17-11 이조빌딩 3층 솔내음한정식*

---

## 📍 후보 3: 제주
### 🌤️ 날씨 요약
- 한국 본토보다 온화한 기온을 유지하지만, 바람이 강할 수 있어 체감 온도는 쌀쌀할 수 있습니다. 맑은 날씨가 많습니다.

### 🥳 주요 행사/축제
- 감귤 따기 체험 (농장별)
- 동백꽃 개화 (일부 지역 시작)
- 한라산 단풍 절정기 이후 겨울 풍경 감상

### 💡 추천 이유
11월 중순 제주도는 늦가을의 정취와 초겨울의 이국적인 풍경을 동시에 경험할 수 있는 최적의 시기입니다. 황금빛 감귤이 가득한 과수원에서 감귤 따기 체험을 하거나, 서귀포 일대에서 피어나는 동백꽃을 미리 만나볼 수 있습니다. 본토보다 온화한 날씨 덕분에 야외 활동을 즐기기에도 부담이 적습니다.

### 🍕 추천 맛집 및 먹거리
- **우진해장국** (한식>향토음식) - *주소: 제주특별자치도 제주시 서사로 11*
- **이춘옥원조고등어쌈밥 제주애월본점** (한식>쌈밥) - *주소: 제주특별자치도 제주시 애월읍 일주서로 7213*
- **모루쿠다 서귀포올레시장점** (한식>해물,생선요리) - *주소: 제주특별자치도 서귀포시 태평로431번길 32 1층 모루쿠다 서귀포올레시장점*

---

## 🛡️ 시스템 에러 및 폴백 로그 요약(errors)
- 에러 없이 모든 시스템이 정상적이고 안정적으로 구동되었습니다.

# 🏗️ Part 1. 프로그램의 핵심 구조: 데이터 파이프라인(Data Pipeline)

이 프로그램은 **데이터 파이프라인(Data Pipeline)** 구조로 설계되어 있습니다.

> **데이터 파이프라인(Data Pipeline)**  
> 공장에서 원자재를 여러 공정을 거쳐 최종 제품으로 만드는 것처럼, 데이터를 여러 단계에서 처리·가공하여 원하는 결과를 만들어내는 흐름을 의미합니다.

본 프로그램의 데이터 파이프라인은 총 **5단계**로 구성됩니다.

```
사용자 날짜 입력
        │
        ▼
Gemini AI 여행지 추천
        │
        ▼
네이버 검색 API 조회
        │
        ▼
Fallback(예외 처리)
        │
        ▼
결과 파일 저장(JSON + Markdown)
```

### ① 날짜 입력받기

사용자가 터미널에서 여행 날짜를 입력합니다.

```
python travel_planner.py --date "2026-10-15"
```

입력된 날짜는 이후 모든 데이터 처리 과정의 기준이 됩니다.

---

### ② Gemini AI에게 여행지 추천 요청

입력된 날짜를 기반으로 Gemini API에 요청을 보내 여행하기 좋은 국내 여행지를 추천받습니다.

---

### ③ 네이버 검색 API 호출

Gemini가 추천한 여행지를 이용하여 네이버 검색 API를 호출하고, 각 지역의 맛집 및 관광 정보를 실시간으로 조회합니다.

---

### ④ 문제 상황 대처(Fallback)

네이버 API가 오류를 반환하거나 검색 결과가 없는 경우 프로그램이 중단되지 않도록 **Fallback** 메커니즘이 동작합니다.

미리 준비된 AI 백업 데이터를 활용하여 결과를 생성하므로 사용자는 끊김 없이 여행 정보를 확인할 수 있습니다.

---

### ⑤ 결과 파일 저장

최종 결과를 두 가지 형태로 저장합니다.

- JSON 원본 데이터
- Markdown 여행 리포트

이를 통해 프로그램 종료 이후에도 결과를 다시 확인하거나 활용할 수 있습니다.

---

# 🛠️ Part 2. 코드 단계별 핵심 용어 및 작동 원리

## 1단계. CLI 인터페이스와 인자 파싱(argparse)

프로그램은 터미널에서 아래와 같이 실행합니다.

```bash
python travel_planner.py --date "2026-10-15"
```

### CLI (Command Line Interface)

마우스로 버튼을 클릭하는 GUI와 달리, **명령어를 입력하여 프로그램을 실행하는 방식**입니다.

### 인자(Argument)

프로그램 실행 시 함께 전달하는 추가 정보입니다.

예를 들어 아래에서

```bash
python travel_planner.py --date "2026-10-15"
```

다음 부분이 인자입니다.

```text
--date "2026-10-15"
```

### 파싱(Parsing)

사용자가 입력한 문자열을 프로그램이 사용할 수 있도록 분석하는 과정입니다.

`argparse` 라이브러리는 입력값에서

```
2026-10-15
```

만 추출하여 변수에 저장합니다.

---

## 2단계. 환경 변수(.env)와 보안 내재화

프로그램에는 API Key가 직접 작성되어 있지 않습니다.

```python
os.getenv("GEMINI_API_KEY")
```

처럼 환경 변수에서 값을 읽어옵니다.

### 환경 변수(Environment Variable)

프로그램 실행 시 사용하는 설정값이나 비밀 정보를 안전하게 보관하는 저장 공간입니다.

### 하드코딩(Hardcoding)

다음처럼 API Key를 코드 안에 직접 입력하는 방식을 의미합니다.

```python
api_key = "ABC123XYZ"
```

이 방식은 GitHub에 업로드하는 순간 API Key가 외부에 노출될 위험이 있으므로 사용하지 않습니다.

### .env 파일

API Key와 같은 민감한 정보를 코드 외부에 저장하는 파일입니다.

예시

```env
GEMINI_API_KEY="..."
NAVER_CLIENT_ID="..."
NAVER_CLIENT_SECRET="..."
```

`python-dotenv` 라이브러리가 실행 시 `.env` 파일을 읽어 환경 변수로 등록합니다.

---

## 3단계. Gemini API 연동과 구조화된 출력(Structured Output)

프로그램은 Gemini API를 호출하여 여행지 추천 정보를 받아옵니다.

### API (Application Programming Interface)

서로 다른 프로그램이 데이터를 주고받기 위한 공식적인 통신 규약입니다.

우리 프로그램은 Gemini API에 요청을 보내고 추천 결과를 반환받습니다.

### HTTP 메서드

#### POST

데이터를 서버로 전송하여 처리나 분석을 요청할 때 사용합니다.

예시

- Gemini에게 프롬프트 전달
- AI 응답 생성 요청

#### GET

서버에 저장된 정보를 조회할 때 사용합니다.

예시

- 네이버 검색 API 조회

### JSON(JavaScript Object Notation)

컴퓨터가 이해하기 쉬운 데이터 형식입니다.

```json
{
  "recommended_city": "제주",
  "weather": "맑음"
}
```

### 구조화된 출력(Structured Output)

AI는 기본적으로 자유로운 문장을 생성하지만, 프로그램에서는 일정한 형식의 데이터가 필요합니다.

예를 들어

```
제주도를 추천합니다!
```

보다는

```json
{
  "city": "제주"
}
```

처럼 일정한 JSON 구조를 반환하도록 프롬프트를 설계합니다.

이러한 방식을 **구조화된 출력(Structured Output)** 이라고 합니다.

---

## 4단계. 매시업(Mashup)과 반복문(for Loop)

Gemini가 추천한 여러 도시를 이용하여 네이버 API를 반복 호출합니다.

### 매시업(Mashup)

둘 이상의 서비스를 결합하여 새로운 서비스를 만드는 기술입니다.

본 프로그램에서는

- Gemini API
- 네이버 검색 API

를 결합하여 여행 추천 서비스를 구현합니다.

### 배열(Array)

Gemini는 여러 개의 도시를 리스트 형태로 반환합니다.

```python
["제주", "강릉", "부산"]
```

### 반복문(for Loop)

리스트를 하나씩 순회하며 동일한 작업을 반복 수행합니다.

```
제주 검색
      ↓
강릉 검색
      ↓
부산 검색
```

이 과정을 자동으로 처리합니다.

---

## 5단계. Fallback 메커니즘

프로그램의 안정성을 높이는 핵심 기능입니다.

### 예외 처리(Exception Handling)

프로그램 실행 중 발생하는 오류를 `try-except` 구문으로 처리하여 프로그램이 비정상 종료되지 않도록 합니다.

### Fallback

외부 서비스가 실패했을 때 준비된 대체 방법을 사용하는 기능입니다.

예를 들어

```
네이버 API 실패
        │
        ▼
AI 백업 데이터 사용
        │
        ▼
최종 리포트 생성
```

프로그램은 중단되지 않고 정상적으로 결과를 제공합니다.

### Graceful Degradation

일부 기능이 실패하더라도 전체 시스템은 계속 동작하도록 설계하는 방법입니다.

실시간 검색이 실패해도 백업 데이터를 활용하여 결과를 생성하는 것이 대표적인 예입니다.

---

## 6단계. 영속성(Persistence)

프로그램 실행 결과를 파일로 저장하는 단계입니다.

### Persistence

메모리에 존재하는 데이터를 프로그램 종료 후에도 사용할 수 있도록 파일 시스템에 저장하는 것을 의미합니다.

생성되는 파일은 다음과 같습니다.

```text
results/travel_report_YYYY-MM-DD.json
```

```text
results/YYYY-MM-DD_travel_plan.md
```

### Markdown(.md)

간단한 문법으로 문서를 작성할 수 있는 경량 마크업 언어입니다.

예시

```markdown
# 제목

## 소제목

- 목록 1
- 목록 2
```

VS Code의 **Open Preview** 기능을 이용하면 보기 좋은 문서 형태로 확인할 수 있습니다.

---

# 💡 요약: 이 과제를 통해 학습한 내용

이 과제를 수행하면서 다음과 같은 실무 중심의 기술을 경험할 수 있습니다.

- Python 기초 문법을 넘어 외부 라이브러리와 API를 연동하는 방법
- AI를 단순한 챗봇이 아닌 시스템의 구성 요소로 활용하는 구조화 기법
- 여러 API를 결합하는 매시업(Mashup) 설계 방식
- 예외 처리와 Fallback을 이용한 안정적인 프로그램 설계
- JSON과 Markdown을 활용한 데이터 저장 및 문서 생성
- 영속성(Persistence)을 고려한 결과 저장 구조
- 실제 서비스 수준의 방어적 프로그래밍과 내결함성(Fault Tolerance) 구현
